# Copy to .env and paste your own token. .env is git-ignored — never commit it.
#
# A fine-grained token with no permissions at all is enough: it does not grant
# access to anything, it only raises the API rate limit from 60 to 5000
# requests per hour.
GITHUB_TOKEN=
